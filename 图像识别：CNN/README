dogs-vs-cats.py 使用CNN
dogs-vs-cats_v2.py 在v1的基础上使用数据增强来生成更多的训练例，防止过拟合
dogs-vs-cats_v3.py 使用预训练的CNN。仅使用特征提取，未使用数据增强
dogs-vs-cats_v4.py 使用预训练的CNN，使用数据增强的特征提取。计算代价很高
dogs-vs-cats_v5.py 在特征提取的基础上，使用微调，即将顶部（最后添加的）的几层解冻，将这几层和新增加的部分联合
                   训练。这里是在v3的基础上使用微调，由于计算代价较大，没有使用具有数据增强v4网络。